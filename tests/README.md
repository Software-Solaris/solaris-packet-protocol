# Tests

## Install dependencies (Debian / Ubuntu)

```bash
sudo apt update
sudo apt install -y build-essential cmake libcgreen1-dev cgreen1
```

## Run the tests

You must be in the repo root (`spp/`, where `CMakeLists.txt` is):

```bash
cd ~/Documents/solaris-software/solaris-v2/spp
make
```
